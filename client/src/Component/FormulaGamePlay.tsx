import React from "react";
import { fullFormulaGame } from "../Page/FormulaPage";

const FormulaGamePlay = ({
  gameId,
  game,
}: {
  gameId: number;
  game: fullFormulaGame;
}) => {
  return <div>Game in progress - component</div>;
};

export default FormulaGamePlay;
