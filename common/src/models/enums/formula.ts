enum CarStateEnum {
  notReady = "N",
  racing = "R",
  retired = "X",
  finished = "F",
};

enum DamageTypeEnum {
  tire = 1,
  gearbox = 2,
  brakes = 3,
  engine = 4,
  chassis = 5,
  shocks = 6,
};

export { CarStateEnum, DamageTypeEnum };
